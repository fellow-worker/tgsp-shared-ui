The input element can be used very basic, just a simple input
```jsx
<Input />
```

The input can also be used as a password input and colors can be adjusted
```jsx
<Input color="secondary" type="password" />
```